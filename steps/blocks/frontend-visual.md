This is a frontend/visual task.
1. Use brand tokens from variables.css — never hardcode colors.
2. Verify the change renders correctly at 1440px, 768px, and 480px viewports.
3. Ensure no horizontal overflow below 480px.
4. Use Bootstrap components consistently. Check existing similar pages for patterns.
5. If i18n is relevant, add translation keys — do not hardcode English strings.
